FROM jenkins/jenkins:lts

USER root

# Install Docker CLI and Git, then clean up apt lists
RUN apt-get update && \
    apt-get install -y docker.io git && \
    rm -rf /var/lib/apt/lists/*

USER jenkins
